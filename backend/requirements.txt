fastapi>=0.110.0,<0.112.0
uvicorn[standard]>=0.27.0,<0.29.0
python-multipart>=0.0.6
pydantic>=2.6.0,<2.8.0
pydantic-settings>=2.2.0,<2.4.0
sqlalchemy>=2.0.25,<2.1.0
PyMuPDF>=1.23.26,<1.25.0
pdfplumber>=0.11.0,<0.12.0
pdf2image>=1.17.0
pytesseract>=0.3.10
easyocr>=1.7.1
Pillow>=10.2.0
faiss-cpu>=1.7.4
sentence-transformers>=2.3.1,<2.7.0
google-generativeai>=0.8.3,<0.9.0
python-dotenv>=1.0.0
numpy>=1.26.0,<2.0.0

# ================================================================
# NEW DEPENDENCIES — Advanced AI Upgrade (v2.0)
# ================================================================

# Feature 1: OpenCV OCR Enhancement Pipeline
# Headless version avoids display dependencies on Windows servers
opencv-python-headless>=4.9.0

# Feature 2: AI Layout Detection (surya-ocr replaces LayoutParser/Detectron2)
# surya-ocr is fully Windows-compatible and CPU-native
surya-ocr>=0.5.0

# Feature 3: CLIP Image Embeddings for Visual Retrieval
open_clip_torch>=2.24.0

# Feature 4: Hybrid BM25 Keyword Retrieval
rank-bm25>=0.2.2

# Feature 5: LayoutLMv3 Multimodal Understanding (optional — large model)
# Required for ENABLE_LAYOUTLM=true; transformers is also used by EasyOCR
transformers>=4.38.0,<4.42.0

# PyTorch — required by surya-ocr, open_clip_torch, LayoutLMv3
# NOTE: If already installed (via easyocr), pip will reuse the existing install.
# For CPU-only Windows install use: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchvision>=0.16.0
